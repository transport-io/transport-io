/**
 * Stage 0 stub entry.
 *
 * This file exists so every CI gate — typecheck, lint, dead-code analysis, pack
 * validation — runs and passes before the first line of library code is written.
 * A version constant is not library code. See DECISIONS.md D24.
 */
export const VERSION: string = '0.0.0'
